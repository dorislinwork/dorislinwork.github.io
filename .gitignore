# 作業系統產生的雜檔
.DS_Store
Thumbs.db
desktop.ini

# 編輯器
.vscode/
.idea/

# 原始設計檔不建議放進 repo（檔案大且不需要上線）
*.ai
*.psd
*.indd
*.sketch
*.fig

# 第一版（方向錯誤的佔位內容）。留在本機當紀錄，但不要進 repo ——
# GitHub Pages 會把 repo 內所有檔案公開，推上去會變成可瀏覽的頁面。
_archive-v1/

# 待處理的原始檔。add-project.mjs 會讀這裡、輸出到 assets/media，
# 原始檔留著不刪但不進 repo（通常是幾十 MB 的未壓縮輸出）。
incoming/*
!incoming/README.md

# Bito 版型原型的產出。正式首頁已經是這個版面了，這個檔只是拿來試新設定用的
# （改 dev/build-bito-proto.mjs 的 PER_ROW 或比例，跑一次就會重新產生）。
# 不進 repo —— GitHub Pages 會公開 repo 內所有檔案，推上去會變成一個過期的重複首頁。
dev/index-bito.html

# 字體對照頁。從 Fontshare 的 CDN 載字只是為了在本機比對，不該上線
# （正式要用哪支字就自己下載自架，不靠別人的 CDN）。
dev/font-compare.html

# 後台「預覽這一頁」的產物。暫存的草稿清單與只產生一頁的預覽檔，
# 都是即時生成的，不該進 repo（GitHub Pages 會把 repo 內所有檔案公開）。
content/_preview-projects.json
work/_preview.html
content/_preview-site.json
_preview-info.html
